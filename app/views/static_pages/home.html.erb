<% if logged_in? %>
    <div class="row">
        <aside class="col-md-4">
            <section class="user_info">
                <%= render 'shared/user_info' %>
            </section>
            <section class="chore_form">
                <%= render 'shared/chore_form' %>
            </section>
        </aside>
    </div>
<% else %>

    <div class="center jumbotron">
        <h1>Hello. My name is Chordle</h1>

        <h2>Stop feeling overwhelmed by the todo list and start getting stuff done.</h2>

        <%= link_to "Let me help!", signup_path, class: "btn btn-lg btn-primary" %>
    </div>
<% end %>